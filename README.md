# A cool virtual assistant

To run the assistant, run the following command:

```bash
# You must have v18.14.0 or higher of Node.js installed

# Select the node version
nvm use

# Install dependencies
npm install

# Run the assistant
node ./src/index.js
```